# Route Resilience AI — Python Dependencies
# Install with: pip install -r requirements.txt

# ── FastAPI & Server ──────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0
websockets==12.0
python-multipart==0.0.9
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
httpx==0.27.0

# ── Database ──────────────────────────────────────────────────
sqlalchemy==2.0.30
aiosqlite==0.20.0          # async SQLite driver
alembic==1.13.1

# ── Configuration ─────────────────────────────────────────────
pydantic==2.7.1
pydantic-settings==2.3.0
python-dotenv==1.0.1

# ── Google Earth Engine ───────────────────────────────────────
earthengine-api==0.1.405
google-auth==2.29.0
google-auth-oauthlib==1.2.0

# ── GIS / Raster Processing ───────────────────────────────────
rasterio==1.3.10
shapely==2.0.4
pyproj==3.6.1
geopandas==0.14.4
fiona==1.9.6

# ── Graph Engine ──────────────────────────────────────────────
networkx==3.3
osmnx==1.9.3
scipy==1.13.0
scikit-image==0.23.2       # skeletonization

# ── AI / Deep Learning (local inference) ─────────────────────
torch==2.3.0               # CPU build; Colab will install CUDA version
torchvision==0.18.0
transformers==4.41.1       # SegFormer via HuggingFace
accelerate==0.30.0

# ── Image Processing ──────────────────────────────────────────
Pillow==10.3.0
numpy==1.26.4
opencv-python-headless==4.9.0.80
albumentations==1.4.8      # data augmentation

# ── AWS / SpaceNet ────────────────────────────────────────────
boto3==1.34.115
awscli==1.32.115

# ── Utilities ─────────────────────────────────────────────────
tqdm==4.66.4
rich==13.7.1               # pretty terminal output
loguru==0.7.2
aiofiles==23.2.1

# ── Testing ───────────────────────────────────────────────────
pytest==8.2.0
pytest-asyncio==0.23.6
httpx==0.27.0              # used for FastAPI test client
